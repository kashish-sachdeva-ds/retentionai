# Runtime dependencies are declared once in pyproject.toml.
# Keeping this compatibility file lets Docker and familiar pip workflows use
# the same locked dependency graph without maintaining a duplicate list.
.
